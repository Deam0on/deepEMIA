# =============================================================================
# THRESHOLDS CONFIGURATION
# =============================================================================
# Detection thresholds, scale bar settings, and measurement options

scale_bar_rois:
  default:
    x_start_factor: 0.7
    y_start_factor: 0.05
    width_factor: 1
    height_factor: 0.05

scalebar_thresholds:
  intensity: 100
  proximity: 50

measure_contrast_distribution: false
